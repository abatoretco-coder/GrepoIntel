"use client";
type City={id:number;name:string;x:number;y:number;points:number;is_ghost:boolean;player_id?:number|null;relation?:string};
export function StrategicMap({cities,onSelect,selected}:{cities:City[];onSelect?:(city:City)=>void;selected?:number}){if(!cities.length)return <div className="map-empty">Aucune cité dans ce filtre.</div>;const xs=cities.map(c=>c.x),ys=cities.map(c=>c.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys);return <div className="strategic-map local-grid" aria-label="Grille de coordonnées Grepolis">{cities.map(city=>{const x=8+84*(city.x-minX)/Math.max(maxX-minX,1),y=8+84*(city.y-minY)/Math.max(maxY-minY,1);const tone=city.is_ghost?"ghost":city.relation?.toLowerCase()??"neutral";return <button title={`${city.name} · ${city.x}|${city.y}`} onClick={()=>onSelect?.(city)} className={`map-city ${tone} ${selected===city.id?"selected":""}`} style={{left:`${x}%`,top:`${y}%`}} key={city.id}><span/></button>})}<div className="map-scale">Grille Grepolis · X / Y · pas une carte terrestre</div></div>}
